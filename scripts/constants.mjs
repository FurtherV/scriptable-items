const MODULE_ID = "scriptable-items";
const LANG_ID = MODULE_ID.toUpperCase();
const TEMPLATES_FOLDER = `modules/${MODULE_ID}/templates`;
const FLAGS = {
  SCRIPTS: "scripts",
};
const TRIGGERS = {
  BUTTON: "button",
  PRE_USE: "preUseItem",
};

export { MODULE_ID, LANG_ID, TEMPLATES_FOLDER, FLAGS, TRIGGERS };
